---
/**
 * Japanese editorial policy marker.
 *
 * Japanese copy must be authored in the source components themselves so that
 * server-rendered HTML, no-JavaScript output, accessibility text and screenshots
 * all expose the same language. This component intentionally emits no runtime
 * text-replacement JavaScript.
 */
---
