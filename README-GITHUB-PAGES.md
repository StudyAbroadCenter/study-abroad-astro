# GitHub Pages deployment

This prototype is configured to deploy automatically to GitHub Pages after changes are merged into `main`.

Expected site URL:

`https://studyabroadcenter.github.io/study-abroad-astro/`

In the repository settings, set **Settings → Pages → Build and deployment → Source** to **GitHub Actions**.

The deployment workflow is stored in `.github/workflows/deploy-pages.yml`.
